name: MASTER MYND transfer history — read only
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  receipt:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Report preserved transfer outcome without downloading or writing
        run: |
          echo 'Direct public API/HTTP attempts failed and did not apply files.'
          echo 'Guest-browser download in run33986473235 succeeded.'
          echo 'Approved ZIP641321bytes; SHA25615ad9feeffc7b6ae65eaf7dc1cba7d9ce1cc2ecf745b919a61a8fe0c6a3928a7.'
          echo 'All38 payload baselines and source/identity/image checks passed before the worker commit.'
          echo 'No importer, content writer, account credential, file download, merge or publication runs here.'
          echo 'Current acceptance is the read-only MASTER MYND reader workflow; release status remains separately verified.'
