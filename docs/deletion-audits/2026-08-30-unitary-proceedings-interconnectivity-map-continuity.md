# 30-Aug-2026 unitary proceedings interconnectivity map — continuity control

## Purpose

This handoff preserves the architecture and evidential boundaries of the proceedings-interconnectivity work independently of the originating ChatGPT thread.

## Controlling architecture

Read together:

- `.github/governance/UNITARY_PROCEEDINGS_INTERCONNECTIVITY_MAP_PROTOCOL_30AUG2026.md`;
- `archive/PROCEEDINGS_MASTER_REGISTER_PROTOCOL.md`;
- `archive/MASTER_PROCEEDINGS_PUBLICATION_GOVERNANCE_30AUG2026.md`;
- `archive/PROCEEDINGS_MASTER_REGISTER.csv`;
- `assets/data/proceedings-interconnectivity-schema-v1.json`;
- `assets/proceedings-interconnectivity-map-20260830.js`;
- `en/proceedings-map/index.html`; and
- `es/mapa-procedimientos/index.html`.

## What changed

The project now treats proceedings interconnectivity as a **map of maps** rather than an aggregate table alone.

The canonical CSV remains the single proceedings/file master. The map derives reader navigation from the public-eligible projection and keeps two relationship classes separate:

1. **direct procedural edges** — explicit parent/child or canonical-ID relationships in controlled relationship fields; and
2. **contextual bridges/lenses** — same recorded track, exact Connection value, geography, chronology or another independently source-controlled bridge.

A context bridge is never silently converted into joinder, coordination, knowledge, wrongdoing, liability or identity of proceedings.

## Reader contract

The bilingual public map supports:

- track/family view;
- approximate chronology/journey;
- single-proceeding trace;
- reverse trace from a child/current file to its recorded origin;
- incoming/outgoing relationship discovery; and
- a plain-language `Why connected?` explanation for each relationship class.

The map remains a navigation surface. Underlying material propositions require the appropriate source.

## Internal digest contract

Future repository-wide proceedings work must traverse the corpus both:

**beginning → end**

and

**end → beginning**.

For each proceeding/file, resolve canonical identity, organ/reference/class, procedural status, parents/children/appeals/referrals/incidents/piezas, professional filing lineage, evidence/events/assets/transactions, chronology context, public routes, contrary evidence and open source gaps. Significant relations should be discoverable in both directions where appropriate.

## Completion boundary

Do not say the proceedings corpus is `complete` or `fully interlinked` merely because all public CSV rows render.

Keep separate:

- node coverage;
- direct-edge coverage;
- context coverage;
- orphan/open-gap count; and
- deployment/live-route coverage.

A proceeding with no direct canonical edge may be genuinely independent or may expose an unresolved relationship gap. The renderer must not manufacture an edge to make the network look complete.

## Continuity / deletion safety

After merge and live verification, the originating chat can be deleted without losing the map architecture, prompt, schemas, public routes or relationship boundaries because those are repository-controlled. This does not authorise deletion of native evidence, private custody sources, backups or other retention-controlled material.

Before claiming final live/deletion-safe status for this implementation, verify the PR merge commit, CI audit, Pages deployment for that exact descendant main and the two public routes/assets.
