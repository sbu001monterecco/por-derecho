# Canonical documentary media desk

Control: `PD-MEDIA-DASHBOARD-20260905-01`. Task #1462; Control Tower #1428. Start with [the current release contract](RELEASE_CANDIDATE.md), current remote main and `ops/PUBLICATION_CONTROLLER.md` before making publication claims or changes.

## Public routes

- Spanish: `es/medios-trazabilidad-relato-publico/#media-desk`.
- English: `en/media-public-narrative-traceability/#media-desk`.

These extend the existing editorial-traceability pages. Do not create the withdrawn `/es/medios/` or `/en/media/` alternatives. Preserve every pre-existing editorial passage, source and qualification outside the uniquely owned additions. The six reading cards, static links and explicit evidence limits are the public product. English labels Spanish sources honestly.

## Build, check, publish

Generate the four owned outputs before committing with `python3 ops/media-dashboard/build_release.py`. CI runs the same command with `--check`, never a content-repair write. Run the tests in this directory and the current repository acceptance gates. `browser_check.py` tests the correct `/por-derecho/` mount or the explicitly selected real host; `verify_live.py` compares the actual public bytes with the merge checkout.

A source test pass is not a merge, Pages deployment or public readback. Read the exact current-head workflow artifacts, controller state and issue closeout. Do not infer deployment from this README, a preview, a source-stage manifest or a historical handover.

## Boundaries and continuity

The earlier proposal is preserved in Git at `61745a557c0a666452cb71f152753c676fa70686`; it is not a current contact register or deployment receipt. The retained `preview-*` files and `public_projection.py` are review fixtures, not production URLs or an authenticated service. This current entry replaces the stale proposal narrative and omits its operational campaign statistics.

Private contact history, endpoint inventories, native messages, provider locators, attachments, delivery classifications and exact outbound approvals stay in owner-controlled private custody, not this source package. Do not reconstruct canonical communication events from aggregate counts. Do not import the old worker's content-writing preparation workflows.

Ask the Record / Pregunta al expediente remains unimplemented as an authenticated service. The public page offers existing contact routes only; no form, upload, automatic answer or automatic sending is provided. Private response, anonymous publication and attributed publication remain separate decisions.

Apply `EMAIL_SEND_FINAL_AUTHORIZATION_RULE.md` and all recipient-class controls before any mail action. A website-deployment instruction sends no email and supplies no exact-package approval. Private-host selection, pending history pagination and the separate media-link-rule worker are not silently closed by this public source release.
