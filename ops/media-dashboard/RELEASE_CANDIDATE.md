# Media source desk implementation — bounded release candidate

Control PD-MEDIA-DASHBOARD-20260905-01. This record advances the 5 September preview to an additive, source-only implementation. It does not claim merge, Pages deployment, private authentication or email transmission.

## Canonical route correction

The complete 5,908-path Git inventory at worker8e4b1e0f found an existing ES/EN editorial-traceability pair and existing incoming homepage links. The source desk therefore extends `es/medios-trazabilidad-relato-publico/` and `en/media-public-narrative-traceability/`, at the shared `#media-desk` fragment. The earlier `/es/medios/` and `/en/media/` suggestions are withdrawn. All prior editorial content, qualifications and outbound links are preserved.

The implementation adds a static, no-JavaScript-readable source desk, six reading routes, exact language switching, evidential distinctions and existing correction/contact routes. It adds no directory, correspondence metrics, forms, file upload, contact capture, AI service, cookie, tracker or email operation. English labels Spanish source pages explicitly. It neither certifies all underlying document accessibility/privacy nor publishes any private campaign history.

Discovery uses the existing `unitary-route-registry-sync-20260819.json` and declared `sitemap-discovery-navigation.xml`, not a parallel search engine. Scoped CSS never affects unrelated routes. The existing site loader and site-wide navigation remain intact. New-source links are verified separately from links inherited in old editorial content.

## Reproducible source transaction

Run `python3 ops/media-dashboard/build_release.py`, then the same command with `--check`. Owned blocks are uniquely marked. The generator may replace only its own blocks, append two controlled search rows and add the two missing sitemap entries; it refuses ambiguous targets or modified canonical-row collisions. Never restore an old whole file over newer main.

The one-shot worker preparation workflow, distinct from the earlier read-only inventory workflow, may commit only these four deterministic outputs to `worker/media-dashboard-plan-20260905`. It cannot merge, deploy or write main. Generated outputs do not trigger its source-path filter. An unexpected dirty path, failed test or non-fast-forward push stops preparation. The active integrator must still reconcile current main and the media link-core worker before publication. This is pre-merge candidate preparation, not post-merge CI content repair.

Acceptance covers scoped tests, exact preservation outside owned blocks, search, mobile/desktop and no-JavaScript rendering, and public-source HTTP/HTML checks. Broad repository diagnostics are captured separately, with exit statuses, and compared against the observed source-main baseline; their collection is not a global green claim. Final integrator CI, merge SHA, Pages run and deployed-byte/browser readback remain independently required.

## Communications and private record

The existing 68-address historical directory and 62-address campaign denominator remain different. History pagination is tracked per person/outlet query; any outstanding cursor keeps that candidate blocked. Website-role verification does not prove historic receipt or current delivery. The exact final approval, core maps/webinar/site-links and native Sent-copy rules remain unchanged. No success, delivery or response state is invented from preparation.

Native messages, addresses, unconfirmed references, source PDFs/maps, manifests and private snapshots stay outside this public tree. The static public desk does not solve private hosting; migration requires a separately selected authenticated destination. Existing Ask the Record approvals remain separate for a private response, anonymous publication and attributed publication.
