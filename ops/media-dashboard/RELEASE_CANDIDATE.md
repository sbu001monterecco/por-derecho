# Canonical media desk release — 5 September 2026

Control: PD-MEDIA-DASHBOARD-20260905-01. Task: #1462; coordination: #1428. The user expressly instructed `deploy`. This document defines the bounded source transaction; it is not itself a merge or live-deployment receipt.

## Current implementation and preserved history

The release continues the existing ES/EN editorial routes at `#media-desk`, not the withdrawn `/es/medios/` or `/en/media/` proposals. Original editorial content, links and qualifications remain byte-identical outside marked owned additions. Six static source cards, language switching, evidence/attribution boundaries, existing corrections routes and existing search/sitemap discovery are added. English clearly labels Spanish source pages. The current README routes to this contract; the earlier proposal remains in historical Git commit61745a5. Retained preview files are historical review fixtures, not a live authenticated service. The current README omits the historical proposal's operational campaign statistics.

There is no private directory, mailbox history, correspondence metric, form, tracker, cookie, upload, AI answer service, email sending or authenticated CRM in the public desk. Ask the Record remains explicitly unimplemented. Private operational registers are not release denominators. No new historical event, communication, identity or source is invented by this navigation layer.

## Proven late-scroll cause and finite repair

Read-only diagnostic run33996371471 rebuilt the61745a5 proposal on main65d56cec and recorded both languages' actual scroll calls. `optimum-reader-journey-finish-20260818.js` invoked its generic restoration after the reader's wheel input, at its later7.2-second apply. The desk's own input-cancellation had already stopped. The previous failing `Reader scroll was overridden` assertion therefore exposed a real inherited interaction, not a reason to remove the test.

The shared module receives only a two-line, new-fragment-specific guard delegating `#media-desk` on `.media-desk-page` to the desk's own input-aware handler. Every other line and target is preserved. The owned page head preloads that shared module using its existing marker and a new cache version, preventing the inherited loader from adding the old cached copy. Existing insolvency note insertion remains outside the new nongeneric section. Mobile menus start closed and close after selection. Sitemap insertion preserves original bytes. Visual review also corrected the desk eyebrow's faint inherited colour by making it inherit the readable body text colour.

## Deterministic source and read-only CI

`build_release.py` writes only its uniquely marked blocks and the two controlled search/sitemap entries. Run it before committing; CI runs `--check` and fails drift rather than repairing it. The previous worker's content-writing preparation workflow is not imported. The final `media-desk-release.yml` contains only checkout, checks and artifacts; no repository writes or merge action.

Acceptance comprises32 local contracts, exact inherited HTML/shared-module/search/sitemap preservation, required preservation/publication/audience checks, syntax checks, and42 browser cases:36 route cases across Chromium/Firefox/WebKit,ES/EN,320/390/1280 widths,JS on/off; six existing-search cases. The original300ms/900ms reader-scroll stability assertion is retained, together with direct fragment, actual menu-selection, note-preservation and no-capture checks. A failure remains a failure, with screenshots and aggregate report. Historical run33996914440 passed32/32 source tests and42/42 browser cases at6c78a6f; a successor must carry its own current-head acceptance rather than borrow that result.

After the controller-authorised merge, a read-only main-push job compares seven real-host resources byte-for-byte with the merge checkout and runs the same42 browser cases against the real public base path. Exact Pages SHA/controller evidence remains independently required. Runtime reports, not this source document, establish which checks actually passed.

## Release boundaries and open work

Preparation baseline65d56cec10e11f5a5c0861bb087ee87e64f5effa includes later JSP,MASTER MYND and CaixaBank work; none is reset. Recheck current main, active lane and exact-head acceptance before claiming the controller permit; reconcile later main before merging. The pending media-link-rule worker, private contact-history pagination, canonical historical communication reconciliation, exact-email approval and private hosting are not silently marked complete by this public-source release. No email or filing is authorised by `deploy`.
