# Unitary backlog clearance — 3 September 2026

**Control:** `PD-PRCLR-20260903-01`  
**Purpose:** keep the active GitHub queue equal to unresolved evidentiary work, not accumulated historical branches.

## Release method

1. Start from exact current `main`.
2. Read the old PR only as a source reservoir.
3. Classify every changed file as current-main collision, exact additive blob, rebuild-required delta, private/restricted material, verifier-only material or obsolete operational state.
4. Reuse exact blobs for source-safe additive files where possible.
5. Rebuild page/index/discovery changes only against current `main`; never restore an old global loader, registry denominator, handover or robots state wholesale.
6. Preserve allegation / documentary fact / inference / contrary evidence / adverse outcome / source gap as separate states.
7. Merge one bounded current-main release, verify the public edge, then close predecessor PRs explicitly as superseded.
8. Keep an old PR open only if unique evidence work genuinely remains.

## Release contents

This release salvages the compact #1212 liquidation-plan/judge/LAJ and Luis López control set, the final #1047 deletion certificate, the unique #790 outbound-email evidence rule and the #769 Orion disclosure audit. It adds a dated repository/website unitary digest and machine-readable PR disposition ledger.

## What this process forbids

- force-merging a stale branch because it is old;
- overwriting newer CAEPR/proceedings/Fiscalía registers with older denominators;
- turning receipt into merits acceptance;
- turning sequence/association/benefit into agreement/participation/intent;
- publishing raw private emails, private locators, signatures, IDs, legal advice or unreviewed full-text personal data;
- describing a page as live until the production URL is read back after merge.

## Clearance objective

The desired steady state is a small active queue consisting only of: (a) source-gap closure, (b) current-main rebuilds with unique evidence, and (c) explicitly blocked items with a named gate. Everything else is merged, archived or closed as superseded.
