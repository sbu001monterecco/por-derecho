# PACKAGE NOTE

This package deliberately uses an anonymised derivative extract rather than republishing the private source report. The complete page-by-page redacted PDF remains a separate pending task because the original attachment must be re-supplied for a complete redaction review.