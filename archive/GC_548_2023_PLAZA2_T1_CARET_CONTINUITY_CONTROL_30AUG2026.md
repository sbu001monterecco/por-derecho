# `548/2023` · Las Palmas de Gran Canaria · `Plaza 2 · T1`

## Caret continuity control — 30 August 2026

**Status:** `PARTIALLY VERIFIED` · `GAP FOUND` · `REPAIRED` at the
registration and public-discovery layer · `UNRESOLVED / SOURCE REQUIRED` for
identity, merits, procedural status and direct inter-proceeding relevance.

## 1. Supplied literals and canonical candidate key

The user supplied:

- reference: `548/2023`;
- geography: Las Palmas de Gran Canaria; and
- locator: `Plaza 2 T1`.

The collision-safe repository key is **`GC-REF-031`**. This is a candidate
reference key, not a declaration that the object is already proved to be a
distinct judicial proceeding.

`Plaza 2 · T1` is retained verbatim as a user-supplied locator. It is not
promoted to an authenticated court name, section, plaza allocation or procedure
type without a primary institutional source.

## 2. Registration and discovery repair

`GC-REF-031` is registered in:

- `archive/PROCEEDINGS_MASTER_REGISTER.csv` as `UNRESOLVED_REFERENCE`, with
  `Is_Proceeding=UNVERIFIED`, `Source_Status=OPEN_REFERENCE` and
  `Public_Treatment=PUBLIC_SUMMARY_WITH_IDENTITY_GAP`;
- `archive/MISSING_EVIDENCE_REGISTER.md` as `ME-111`; and
- the bilingual public Master Proceedings Register and Proceedings Map.

PR #1236's historical `LIVE_VERIFIED` evidence covers the canonical-CSV runtime
projection that was then deployed. PR #1235 separately migrates those browser
surfaces to the deterministic allowlisted derivative
`assets/data/proceedings-master-public-v1.json`. That migration is
`LIVE_VERIFIED`: reviewed head `40ccc3c699bcc1147a9ac65a52e93fec240633ce`,
reviewed/merge tree `c64ae7547fed024ad0e82397f09fc5f61e2f5da7`, merge
`e13652bb8b3f51dd050c431a58e2bd70b83f5676`, Pages run `33342771113` / #1314
and bounded live readback. It does not alter the source-pending identity
boundary, create a caret or establish a direct/contextual proceeding edge.

The same canonical row drives the English and Spanish public register and map.
This gives reciprocal discovery between the register and map without creating
a second identity record or a stale handwritten duplicate.

Public routes:

- `/en/master-proceedings-register/`
- `/es/registro-maestro-procedimientos/`
- `/en/proceedings-map/`
- `/es/mapa-procedimientos/`

No dedicated case page is created while the case object, parties and relevance
remain unidentified.

## 3. Identity and caret decision

The repository and connected-source review did not locate a signed/native case
header, NIG, complete procedure reference or equivalent primary identity
record. The numeric reference is not unique across public Spanish materials.

Accordingly:

- no `PD-SP-R-*` CAEPR identity is admitted;
- any presentation caret remains **`CARET_PENDING`**;
- `GC-REF-031` is the only canonical candidate key for this lead; and
- a later primary source must update this row rather than create a parallel
  `548/2023` record.

## 4. Context and relevance boundary

The presently verified relevance is limited to a user-supplied Gran Canaria
judicial-reference lead requiring continuity control.

No source presently establishes a relationship to Concurso 36/2012, Sun Park,
an existing person/entity identity, an appeal, an incident, a filing or another
proceeding in the register. The master row therefore has no
`Parent_Master_ID`, `Appeal_or_Review` or `Linked_Proceedings` edge.

The map may expose `GC-REF-031` beside other Gran Canaria material as geographic
navigation context. That proximity is not an evidential or procedural link and
must not be described as one.

## 5. Source and professional-lineage gaps

Before identity or direct links can be promoted, obtain and reconcile:

1. a signed/native case header, notice or certified docket extract;
2. the NIG, exact judicial section/plaza and complete procedure class/reference;
3. parties, procedural capacities, object and current status;
4. counsel, procurador, personación, substitutions and service chain;
5. filings, orders, judgments, appeals/reviews and finality; and
6. primary evidence supporting each proposed relationship to an existing Por
   Derecho matter.

These requirements are controlled as `ME-111`.

## 6. Provenance and finite-search result

The lead originates in Gil Marer's 30-Aug-2026 instruction. That instruction is
provenance for the lead, not primary institutional proof of its identity.

A finite scan of the repository, connected Gmail and Drive sources, and public
sources found no exact primary case header for the supplied combination. The
search result supports an open-source gap only; it does not prove that the
proceeding or document does not exist.

## 7. Publication, update and deletion safety

The public projection must preserve the `UNVERIFIED`/`OPEN_REFERENCE` status and
the open identity gap. Publication makes the lead discoverable; it does not
convert it into a judicial finding or an established case nexus.

Deployment closeout: PR #1236 merged reviewed head `42ae5a71…` as
`0307015ef99ae0560a6a3f12c99afb966bc82e7a`. GitHub Pages run `33338753328`
/ #1301 completed successfully on that exact merge. A later cache-busted
readback returned HTTP 200 and exact merged bytes for the canonical CSV and all
four bilingual register/map route files; the live CSV field audit confirmed the
open identity status and empty direct-edge fields. This upgrades only the
publication lifecycle to `LIVE_VERIFIED`; `CARET_PENDING`, `ME-111` and every
substantive source gap remain open.

Future source recovery must update `GC-REF-031`, `ME-111`, the master row and
every material public projection together. If the source proves a duplicate,
the candidate must be retained as an alias/provenance record and redirected to
the surviving canonical identity; it must not be silently deleted. Native
evidence and retention-controlled material are outside any deletion authority
created by this control.
