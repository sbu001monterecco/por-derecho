# CARET (`^`) SHORTHAND AND CAEPR DISAMBIGUATION PROMPT

**Control date:** 27 August 2026  
**Status:** mandatory startup prompt for new repository-maintaining threads  
**Scope:** ChatGPT/Codex conversational shorthand, CAEPR identity rendering, and Juan José Vera Díaz LinkedIn-profile metadata

## 1. Mandatory interpretation in every new thread

When Gil Marer uses a caret, first classify which of the following three forms applies. Do not silently merge them.

### A. Standalone conversational caret: `^`

A standalone `^` points upward to the immediately preceding assistant proposal, recommendation, instruction, action bundle or clearly identified item. It means:

> **Apply, execute, continue or confirm the immediately preceding item within the current stated scope, subject to the normal evidence, authority, safety and publication gates.**

Do not treat a standalone `^` as a CAEPR identity assertion. If the immediately preceding item contains several clearly grouped actions, execute the grouped package unless the user narrowed the scope.

### B. Group shorthand: `ALL ^`

`ALL ^`, `all ^`, or materially equivalent wording means:

> **Apply or execute all of the immediately preceding grouped recommendations/actions that remain within the current authorised scope.**

It is still not, by itself, an identity-verification result. Preserve any unresolved evidential or operational exceptions.

### C. CAEPR identity caret attached to a label: `Name^`

A caret immediately attached to a person, organisation, institution or proceeding label keeps its existing CAEPR meaning:

> the displayed object has been positively identified and reconciled to one immutable CAEPR identity record for that stated context.

Examples: `CaixaBank, S.A.^`, `Juan José Vera Díaz^`, `Concurso 36/2012^`.

This identity marker does **not** prove conduct, role on every date, receipt, knowledge, relationship, coordination, ownership, control, participation, liability, guilt, current status, procedural outcome or source authenticity.

## 2. Explicit CAEPR verification commands

Treat **`confirm all is^`**, **`CAEPR^ confirmation`**, or an equally explicit request to verify caret identity coverage as a CAEPR verification command. Run the finite reconciliation protocol in `.github/governance/CAEPR_CARET_IDENTITY_AND_ALL_IS_VERIFICATION_PROTOCOL_26AUG2026.md` and report denominator, confirmed count, pending/suspended exceptions and scope.

Never convert a conversational standalone `^` into an identity confirmation merely because the same symbol is used by CAEPR.

## 3. Juan José Vera Díaz — LinkedIn profile metadata correction

The wording previously described in some working analysis as a **“CAIC / Comunidad de Madrid identity”** is not a separate matter-relevant identity lane and must not be treated as one.

The source context is the reviewed LinkedIn profile screenshot. In that screenshot:

- **`CAIC`** appears as profile/professional-affiliation text;
- **`Community of Madrid, Spain`** appears as LinkedIn geographic location metadata.

For this matter, **`Community of Madrid, Spain` has no evidential or investigative relevance** unless a separate independent source later establishes a specific material connection. It must therefore not be used to infer or investigate employment by the Comunidad de Madrid, public office, institutional influence, public-sector relationships, or any other connection to the Comunidad de Madrid.

If the screenshot is transcribed, the location string may be preserved only as source-literal profile metadata. Do not promote it into the actor map, relationship graph, causation model, institutional perimeter, OSINT target list or public allegation.

The relevant person-identity continuity remains **Juan José Vera Díaz^** in the Meeting Point / FTI / current CAIC professional-history context, with date-specific capacities and exact legal entities proved separately.

## 4. Startup rule

Every new repository-maintaining thread that reads `ops/CURRENT_STATE.json` must treat this file as the controlling disambiguation prompt for `^`. Where older text conflicts with this prompt on the conversational meaning of standalone `^` or on the relevance of `Community of Madrid, Spain`, this prompt supersedes that older working interpretation while preserving the old source literal for provenance.
