# Verification plan — obligation-thread closeout — 21 August 2026

Before merge:

- confirm branch remains based on current `main` or reconcile any concurrent merge;
- inspect changed-file list for private data, credentials and privileged detail;
- ensure the national LAJ rejection correction is present;
- ensure the Anticorrupción signature blocker is present;
- ensure the RICPE new-information control is present without portal credentials;
- ensure the already-sent matrix prevents duplicate filings;
- ensure the private Gmail closeout is referenced but not reproduced;
- run repository checks applicable to Markdown-only continuity changes.

After merge:

- verify the handover and deletion marker exist on `main`;
- record final merge SHA;
- classify originating thread as `DELETION-SAFE WITH OPEN ACTIONS`.